# Development rules
Only work inside this repo, and only create directories further down the tree than the statful directory.
Security is very important. Make sure that security is being considered while developing. Since we work with agents too, make sure that what they have access to is considered to prevent things like promt injection.